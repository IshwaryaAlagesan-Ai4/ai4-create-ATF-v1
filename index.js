#!/usr/bin/env node
const chalk = require('chalk');

console.log(' 🔌 ai4 ATF Complete Testing  🌟');

console.log(
    chalk.yellow(
      `You are using Node ${process.version} so the project..\n`
    )
  );